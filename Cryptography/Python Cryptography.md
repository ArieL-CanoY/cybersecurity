```python
# chr() - convert ASCII number to character 
print(chr(97))  # a

# ord() - convert character to ASCII number
print(ord('a'))  # 97



# byteValue.decode() - decode a byte 
byteVal = b'hello world'
print(byteVal.decode())  # hello world


```