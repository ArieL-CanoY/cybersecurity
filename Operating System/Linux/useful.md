```python
>> apropos <purpose of command>
	- used for finding commands that do specific purpose
	- search for description when using the command "whatis <command>"
	- useful when piping to grep command
		ex. apropos user | grep add

>> man <command>
	- show the reference manual for specific commands
		ex. man ls

>> curl https://cheat.sh/<commmand>
	- simplified explanation for specific commands if the man pages overwhelmns you


other:
	>> by running a command with ampersand (&), you can continously run application execute from terminal by pressing ctrl + d.
	>> problem: No X11 Display variable was set
	>> solution: $ export DISPLAY=:0.0
```







